# Repo Cleanup Decisions

**Round:** 2 — Repository Cleanup + Hygiene
**Status:** COMPLETE
**Date:** 2026-02-16

---

## Files Moved to `archive/legacy/`

| File/Directory | Original location | Reason | Rollback |
|---|---|---|---|
| `api_secondary/` (routes + models) | `api/routes/`, `api/models/`, `api/config.py` | Superseded by `app/api/` after Round 1 canonicalization. Dead code once `api/server.py` was updated to import from `app/`. | `mv archive/legacy/api_secondary/* api/routes/` etc. |
| `reconx_enterprise/` | `reconx-enterprise/` | Duplicate of the main stack. All unique content (if any) is already in `app/`. | `mv archive/legacy/reconx_enterprise/reconx-enterprise .` |

---

## Files Deleted

| File | Reason |
|---|---|
| `.DS_Store` | macOS metadata artifact; already in `.gitignore` |
| `test.db` | Stale test database artifact; generated by test suite |

---

## Files Kept (Reviewed, Intentionally Retained)

| File | Reason |
|---|---|
| `api/server.py` | Canonical backward-compat entrypoint (`uvicorn api.server:app`) |
| `api/__init__.py` | Python package marker needed for entrypoint module |
| `backend/` (all shims) | Backward-compat re-exports for existing scripts/docs |
| `reconx.db` | Live database for development |
| `data/rate_limits.db` | Live rate-limit tracking DB |

---

## `.gitignore` Additions

Added the following patterns:
- `test.db` — test artifact created by pytest suite
- `/tmp/unref_*.txt` — output from `find_unreferenced_files.sh`
- `.docker/` — Docker build cache
- `alembic/versions/*.pyc` — compiled migration files

---

## Round 2 Gate Results

| Gate | Result |
|---|---|
| `python3 -m compileall -q .` | PASS |
| `.venv/bin/python -m pytest -q` | PASS (139 passed) |
| `.venv/bin/python -m pytest -q tests` | PASS (30 passed) |
| `bash scripts/find_unreferenced_files.sh` | PASS (no errors) |
| `git status --short` | Clean (expected new files only) |
